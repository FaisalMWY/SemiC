import 'package:flutter/material.dart';

class IdentityAndLogo extends StatelessWidget {
  const IdentityAndLogo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
